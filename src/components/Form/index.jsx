import React from "react";
import Input from "../Input";

import "./form.css";

const Form = () => {
  return (
    <form className="form">
      <Input placeholder="Write something..." />
    </form>
  );
};

export default Form;
